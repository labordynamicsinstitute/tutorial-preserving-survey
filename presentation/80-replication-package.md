## What the final replication package looks like

<!-- TODO (Lars): fill in — show the layout of the replication package for this simple example:
     the cleaned data, the code, the survey definition (.qsf), the download logs, and the
     checksums that tie it all together. -->
